{
  "name": "attendiqo-firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node --test firestore.rules.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
